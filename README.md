This program currently assumes that the map starts with a forehand hit and that the parity is well defined (no resets due to extreme angles, only due to bombs).

To run the console app you must pass in the path to a difficulty.
